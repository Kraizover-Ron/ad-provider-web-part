PORT=5000
FIREBASE_DATABASE_URL=
ADMIN_KEY=

# Option 1: provide service account JSON via env var (recommended)
FIREBASE_SERVICE_ACCOUNT_JSON=

# Option 2 (fallback): place service account file at:
# cookieclicker-server/secrets/firebase-service-account.json
